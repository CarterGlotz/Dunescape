node_modules
dist
.vite
.env.local
.env.*.local
.claude/
logs/*.log
context/.session-lock

# Private surfaces — never commit (sanitization 2026-06-03)
ignis/output/
docs/CREATIVE_DIRECTION_RECORD.md

# Local operational surfaces — never commit (S63 hygiene)
secrets/
.cache/
.ops-cache/
docs/cache-ledger.ndjson

# Unassigned work surface — keep untracked until separately assigned (S77)
obelisk-passport/
