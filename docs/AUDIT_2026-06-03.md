<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-03 -->
<!-- project: solara · type: game -->

# Project Audit — Solara

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 66.6
- Top 3: director-route-briefs · outcome-receipts · blocker-preflight-repair

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 10 | 9 | 28.4 | **director-route-briefs** — Turn Director route segments into playable briefings. The Director already computes route segments, shrine chance, rival chance, and pressure, but the player-facing layer mostly shows raw pressure. Converting those mechanics into compact tactical route briefs makes the daily dungeon feel authored without paid generation. **Recipe:** Extend `src/game/directorMechanics.js` with deterministic segment goals, danger labels, reward tells, and route summary text; surface the summary through chronicle/feed data and tests. |
| 2 | 🔥 | user-feedback-loop | 2h | 9 | 8 | 22.7 | **outcome-receipts** — Add deterministic outcome receipts for player/world actions. The startup brief reports 0/15 receipts/outcomes, and the current UI explains actions but does not produce a reusable receipt contract. A public-safe receipt layer lets Solara close the loop after runs, offerings, reactions, and world-feed clicks while feeding Studio integrations without new token cost. **Recipe:** Add a `src/game/outcomeReceipts.js` module, include receipt summaries in public chronicle integration data, and cover it with unit tests. |
| 3 | ⚡ | security | 1h | 8 | 4 | 16.0 | **blocker-preflight-repair** — Repair blocker-preflight so human-blocker discipline works. `node scripts/blocker-preflight.mjs` currently fails because `scripts/lib/blocker-rules.mjs` is missing. That breaks CANON-019 evidence gathering and makes the Supabase DB URL blocker harder to classify cleanly. **Recipe:** Create the missing blocker rules helper with conservative classification for signup-only, credential, deploy, migration, and agent-attemptable tasks; rerun the preflight. |
| 4 | ⚡ | ui-ux | 1h | 7 | 6 | 14.8 | **world-feed-action-results** — Make world-feed clicks return structured action results. World feed items are clickable, but action handling is inline in `App.jsx` and only logs a terse console line. A structured action-result module makes every feed click produce a consistent route, tab, target, and receipt-ready summary. **Recipe:** Add an exported world-feed action resolver, use it in App.jsx, and test tab/map/setting outcomes. |
| 5 | ⚡ | token-api-consumption | 30m | 7 | 5 | 13.2 | **token-cost-guardrail** — Publish a zero-token runtime cost guardrail in public status. Solara already has a strong deterministic policy, but the status/chronicle contract should make the cost boundary impossible to miss. This protects the pre-revenue free-tier posture while allowing future server-side cached summaries to be gated deliberately. **Recipe:** Extend intelligence policy/status output with a runtime cost guardrail object and assertions that browser token cost remains 0. |
| 6 | ⚡ | security | 1h | 7 | 5 | 12.4 | **backend-readiness-digest** — Expose backend readiness without leaking secrets. Solara has a clear live backend gap: read probes work, hardened RPCs are not deployed, and `SUPABASE_DB_URL` is missing. A deterministic readiness digest lets UI, public JSON, and handoff docs say exactly what is safe now without exposing or depending on secrets. **Recipe:** Add a public-safe backend readiness module that summarizes local/live/hardened modes, required non-secret next action labels, and player-facing copy; wire it into chronicle exports and tests. |

## Innovation reserve

- director-route-briefs
- outcome-receipts

## Skipped

- **full-browser-playwright-runner** — High value but requires adding or activating browser automation infrastructure; defer until this pass closes deterministic module gaps.
- **deploy-supabase-hardening** — Still depends on a deploy-capable `SUPABASE_DB_URL` that current evidence says is missing; keep as blocker with preflight evidence.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| blocker-preflight-repair | shipped | Added `scripts/lib/blocker-rules.mjs` and `scripts/lib/task-board.mjs`; `node scripts/blocker-preflight.mjs --json` now exits 0 and reports only the Supabase DB URL blocker. |
| director-route-briefs | shipped | Daily Rite route segments now include tactic, danger, encounter labels, reward tells, route goals, boss brief, and route summary; covered by unit and chronicle tests. |
| world-feed-action-results | shipped | Added `resolveWorldFeedAction()` and wired `App.jsx` to consume structured tab/map/message results. |
| outcome-receipts | shipped | Added deterministic public-safe receipt generation and exposed receipt summaries through chronicle integrations with zero browser token cost. |
| backend-readiness-digest | shipped | Added public-safe backend readiness digest and surfaced staged live-read/table-fallback mode in status/chronicle contracts. |
| token-cost-guardrail | shipped | AI policy and intelligence digest now expose `runtime_cost_guardrail`; tests assert browser token cost remains 0. |
| dependency-audit-cleanup | shipped | Package-trust checked Vite 6.4.3 and patched transitive audit fixes; `npm audit --json` reports 0 vulnerabilities. |

---
*Generated by `/audit` v1.0 · ready for `/implement`*
