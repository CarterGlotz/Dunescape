<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-04 -->
<!-- project: solara · type: game -->

# Project Audit — Solara

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 10
- Combined Priority: 277.7
- Top 3: sun-almanac · sundial-queue · legacy-vows

## Signals (personalized)

- The 2026-06-03 audit shipped 7/7 items — this plan is net-new, not a board re-list.
- `SUPABASE_DB_URL` is the only human blocker (Carter-only); the genius response is to make offline-first a *feature* (see sundial-queue), so the day the secret lands becomes a sync launch moment.
- `src/App.jsx` is 4,016 lines — under the 5,000-line convention threshold; this pass adds modules + thin wiring, no risky split.
- All new intelligence stays deterministic and zero-token per the 2026-04-17 decision.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 10 | 9 | 45.0 | **sun-almanac** — Deterministic 7-day Director forecast. Tomorrow's pressure/modifiers are already computable from the date seed; publishing a week of shrine-heavy days, rival surges, and boss peaks creates appointment gameplay nobody in this category has. **Recipe:** add `src/game/almanac.js` over the existing Director seed path; surface an Almanac strip on the Daily Rite tab; export via chronicle/status; determinism + zero-token tests. |
| 2 | 🔥 | feature-depth | 2h | 9 | 9 | 40.5 | **sundial-queue** — Offline outbox that converts the Supabase blocker into a feature. Trust-sanitized queue for graves/offerings/echoes/reactions with receipts, flushing through the RPC-first path when the backend link goes live. **Recipe:** add `src/game/sundialQueue.js`; wire `sharedWorldService` enqueue-on-disconnect + flush-on-connect; test caps, sanitization, ordering, receipts. |
| 3 | 🔥 | gamification | 2h | 9 | 9 | 40.5 | **legacy-vows** — Pre-run pledges that amplify or dim your grave's legacy. Kept vows boost the grave's constellation offering value; broken vows dim it — every death becomes an authored epitaph. **Recipe:** add `src/game/vows.js` (seeded offers, keep/break evaluation, legacy multipliers); wire into Rite start + debrief + grave payload; tests. |
| 4 | ⚡ | ai-intelligence | 1h | 8 | 7 | 35.3 | **director-memory** — "The Sun remembers you": rolling local run record deterministically biases personal route danger/reward tells. Self-tuning difficulty, zero server, zero tokens. **Recipe:** add `src/game/directorMemory.js` (clientStore window + bounded bias); one remembrance line in route brief; tests. |
| 5 | ⚡ | ai-intelligence | 2h | 8 | 8 | 32.0 | **chronicle-scenes** — "The Myth So Far": seeded template-grammar vignettes composed from chronicle events. Deterministic AI as authorship — story-so-far for new players, narrative session delta for returners. **Recipe:** add `src/game/chronicleScenes.js`; Myth So Far panel + chronicle export; determinism + identity-safe naming tests. |
| 6 | ⚡ | user-feedback-loop | 2h | 8 | 8 | 32.0 | **challenge-links** — "Last Light" shareable challenge URLs encoding {date, seed, score, vow}. Friend opens the same deterministic route with a "beat their light" banner — zero-cost acquisition loop. **Recipe:** add `src/game/challengeLinks.js` encode/decode with tamper rejection + expiry; boot-time param read; debrief comparison stamp; tests. |
| 7 | ⚡ | security | 1h | 6 | 5 | 18.9 | **save-fuzz-tests** — Seeded fuzz harness over save import sanitization: hostile strings, wrong types, huge arrays, prototype-pollution shapes. Locks in resilience before more save-touching features. **Recipe:** add `tests/save-fuzz.test.mjs`; assert never-throws, bounded fields, current save version. |
| 8 | ⚡ | ui-ux | 2h | 8 | 4 | 16.0 | **hud-text-scale** — Persisted text-scale setting + HUD hierarchy/contrast pass. Cheapest accessibility win, touches every minute of play; board has flagged it 3 sessions running. **Recipe:** clientStore-persisted scale applied as root font-size variable; bump lowest-contrast HUD labels. |
| 9 | 💡 | speed | 1h | 6 | 3 | 11.4 | **menu-chunk-split** — manualChunks + React.lazy for display-only lore/menu panels to trim first-load cost on the new-player path. **Recipe:** rollup manualChunks + lazy `MenuLorePanels`; compare build sizes. |
| 10 | 💡 | organization | 30m | 4 | 2 | 6.1 | **board-hygiene-worktree** — Close the already-satisfied "resolve dirty worktree" Next item (git is clean) and record the 5 lib modules propagated from studio-ops this session. **Recipe:** move to Implemented Ideas with evidence; sync Now/Next with shipped reality. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- sun-almanac · sundial-queue · legacy-vows · chronicle-scenes · challenge-links

## Skipped

- **deploy-supabase-hardening** — still gated on the `SUPABASE_DB_URL` GitHub secret (Carter-only); preflight evidence already proves clean failure. Flagged, not blocking.
- **full-browser-playwright-runner** — browser automation infra out of scope for one pass; deterministic smoke + new fuzz tests carry validation.
- **app-jsx-full-split** — App.jsx at 4,016 lines is under the 5,000-line convention threshold; modules + thin wiring only.

## Execution Log (2026-06-04 /implement pass)

| Slug | Status | Evidence |
|---|---|---|
| director-memory | ✅ shipped | 5-run window + bounded bias applied in startDailyRun; remembrance line in run chat; all run-end hooks record memory; tests green |
| sun-almanac | ✅ shipped | Deterministic 7-day forecast + phase-drift watch; Daily Rite tab strip + `shared_world.almanac` export; determinism tests |
| legacy-vows | ✅ shipped | Seeded vow offers, pledge picker, keep/break evaluation, grave epitaph stamping, debrief lines; legacy-math tests |
| sundial-queue | ✅ shipped | Offline outbox wired into sharedWorldService; flush-on-connect in Supabase loader; queue card on Daily Rite tab; flush/retention tests |
| chronicle-scenes | ✅ shipped | Myth So Far panel + `shared_world.myth_scenes` export; fixed ordinal + season-day alignment in status script |
| challenge-links | ✅ shipped | Checksummed URL tokens, boot banner, run-end comparison, Copy Challenge Link button; tamper/expiry tests |
| save-fuzz-tests | ✅ shipped | 250-seed hostile fuzz + pollution checks; **found and fixed a real `Number([Symbol()])` crash** in save.js |
| hud-text-scale | ✅ shipped | XL (1.3) scale option in both settings rows + preference validation |
| menu-chunk-split | ✅ verified pre-existing | lazy MenuLorePanels + manualChunks already live; build confirms menu-lore 6.35 kB split |
| board-hygiene-worktree | ✅ shipped | Stale worktree item closed; propagated lib repairs recorded |

**Validation:** 45/45 unit tests (was 31) · production build ✓ · smoke runtime ✓ · chronicle exports verified.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
