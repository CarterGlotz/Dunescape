<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-07 -->
<!-- project: solara · type: game -->

# Project Audit - Solara

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 162.7
- Top 3: rpc-contract-manifest · sundial-queue-briefing · rite-pacing-coach
- Personalization: builds directly on the shipped Sun Almanac, Sundial Queue, Legacy Vows, Director Memory, Myth So Far scenes, and Last Light challenge links.
- Blocker posture: `SUPABASE_DB_URL` remains the public-scale backend hardening gate; agent work should make the contract explicit and the offline path trustworthy.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | fire | security | 1h | 9 | 7 | 36.0 | **rpc-contract-manifest** - Make backend hardening verifiable before the secret lands. The verifier currently proves public reads and two hardened RPCs, while runtime now depends on five write contracts. **Recipe:** add a reusable backend contract manifest, make `scripts/verify-supabase-hardening.mjs` consume it, export the manifest summary through public contracts, and test that every queued write kind has a matching RPC. |
| 2 | fire | user-feedback-loop | 1h | 8 | 8 | 32.0 | **sundial-queue-briefing** - Show exactly what offline light is waiting to sync. The queue is a feature, but players need grouped record counts, oldest age, and next sync condition to trust it. **Recipe:** extend `src/game/sundialQueue.js` with a public-safe briefing object, surface it in the Daily Rite card and chronicle export, and test grouping/safety. |
| 3 | fire | gamification | 1h | 8 | 8 | 32.0 | **rite-pacing-coach** - Turn the Director route plan into mid-run decisions. The route plan is strongest before the run; the coach makes it useful during the run by reading wave, vow, challenge target, and current segment. **Recipe:** add `src/game/riteCoach.js`, wire it into run/debrief copy, and test deterministic guidance. |
| 4 | high | engagement | 1h | 7 | 7 | 28.0 | **last-light-result-card** - Make every Daily Rite shareable without a screenshot. Challenge links are replayable, but a compact text result card gives Solara a daily social artifact. **Recipe:** add `src/game/resultCard.js`, use it in the completed Daily Rite copy action, export the shape through chronicle contracts, and test stable sanitized formatting. |
| 5 | high | ai-intelligence | 30m | 7 | 6 | 26.0 | **outcome-receipt-queue-link** - Connect pending writes to the zero-token intelligence layer. Receipts should acknowledge sealed offline graves/scores/offerings before Supabase accepts them. **Recipe:** add a `queue_pending` receipt when a queue briefing is present; test token cost remains zero. |
| 6 | high | speed-organization-efficiency | 1h | 6 | 5 | 8.7 | **app-surface-smoke-contract** - Stop regressions in the newly modular Daily Rite surface. Unit tests can pass while App.jsx stops exposing the new systems coherently. **Recipe:** add a focused smoke contract for World Feed, Almanac, Myth So Far, Vow picker, queue card, challenge copy, result-card, and coach markers. |

## Innovation reserve

- sundial-queue-briefing
- rite-pacing-coach
- last-light-result-card

## Skipped

- **deploy-supabase-hardening** - Still depends on `SUPABASE_DB_URL` or owner dashboard credentials.
- **full-app-jsx-split** - Safer after a smoke contract protects the Daily Rite surface.
- **paid-ai-narration** - Not compatible with free-tier cost discipline before revenue.

## Execution Log

| Slug | Status | Evidence |
|---|---|---|
| rpc-contract-manifest | shipped | `src/game/backendContract.js` defines required shared-world tables/RPCs; `scripts/verify-supabase-hardening.mjs` consumes it; public chronicle/status exports `backend_contract`; tests verify every queue kind has a matching RPC. |
| sundial-queue-briefing | shipped | `getSundialQueueBriefing()` groups sealed records, oldest age, sync condition, and cap data; App.jsx surfaces it in the Daily Rite queue card; public contracts export `shared_world.sundial_queue`. |
| rite-pacing-coach | shipped | `src/game/riteCoach.js` turns route segment, wave, vow, and challenge pressure into deterministic guidance; App.jsx announces and displays coach guidance during/after a Daily Rite. |
| last-light-result-card | shipped | `src/game/resultCard.js` builds sanitized Last Light cards; Daily Rite completion/failure share cards now use it; chronicle exports `result_card_shape`. |
| outcome-receipt-queue-link | shipped | `buildOutcomeReceiptSet()` emits a zero-token `queue_pending` receipt when sealed writes are waiting. |
| app-surface-smoke-contract | shipped | `scripts/smoke-runtime.mjs` now asserts the Daily Rite modular surface markers; `npm run smoke` passes. |

Validation: `npm test` passed 50/50, `npm run build` passed, and `npm run smoke` passed.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
