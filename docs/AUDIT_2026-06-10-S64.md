<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-10 -->
<!-- project: solara · type: game · session: 64 -->

# Project Audit — Solara

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 119.0
- Top 3: pg-connection-truth-gate · rite-consequence-engine · feedback-next-action-digest
- Personalized flags: stale `SUPABASE_DB_URL` surfaces contradict the `PG_CONNECTION_SOLARA` decision; Daily Rite rooms need consequence depth; feedback ledger needs a next-action loop; `src/App.jsx` remains 4,229 lines.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 30m | 9 | 7 | 42.0 | **pg-connection-truth-gate** — Align workflow, contracts, tests, and generated public JSON to `PG_CONNECTION_SOLARA`. The current code still advertises `SUPABASE_DB_URL` even though the project decision says Solara must use a project-scoped Postgres connection for cloud project `fjnpzjjyhnpmunfoycrp`. **Recipe:** Update backend readiness/contract modules, workflow preflight, tests, generated public outputs, and task-board language. |
| 2 | 🔥 | gamification | 1h | 9 | 8 | 36.0 | **rite-consequence-engine** — Turn Director room segments into deterministic risk/reward receipts. The route weaver now drives rooms, but the game still needs shared consequence copy for entry, clear, failure, rewards, and next action. **Recipe:** Add a Daily Rite consequence module, wire it into run start/transitions/death/clear share-card myth lines, and test it. |
| 3 | ⚡ | user-feedback-loop | 45m | 8 | 7 | 28.0 | **feedback-next-action-digest** — Convert capped local feedback events into a zero-token recommendation loop. Solara counts starts, ends, shares, and repairs, but the runtime should explain what action is missing next. **Recipe:** Extend `feedbackLedger`, expose the digest in chronicle/status integrations, render it on the front door, and test sanitization. |
| 4 | ⚡ | speed-organization-efficiency | 45m | 6 | 5 | 13.0 | **daily-run-session-extraction** — Pull Daily Rite run object assembly out of `App.jsx`. Full combat extraction is too risky here, but the Daily Rite session factory is now stable enough to move. **Recipe:** Add a `dailyRunSession` module, use it from `App.jsx`, and cover it with Node tests. |

## Innovation reserve

- rite-consequence-engine
- feedback-next-action-digest

## Skipped

- **deploy-supabase-hardening** — Still requires `PG_CONNECTION_SOLARA` or owner SQL-editor access.
- **paid-ai-myth-narration** — Still conflicts with deterministic-first, zero-browser-token, free-tier cost-neutral posture.
- **full-canvas-combat-split** — Worth doing later, but risky before one more guarded Daily Rite extraction lands.

## Execution Log

- **pg-connection-truth-gate** — shipped. Active source, tests, workflow, docs, and generated public JSON now consistently use `PG_CONNECTION_SOLARA`; `rg` found no remaining `SUPABASE_DB_URL` references in active source/tests/workflow/public output.
- **rite-consequence-engine** — shipped. `src/game/dailyRiteConsequences.js` turns Director route segments into deterministic entry/clear/failure/share consequences, and `src/App.jsx` surfaces those lines through Daily Rite flow.
- **feedback-next-action-digest** — shipped. `summarizeFeedbackLedger()` now includes a zero-token `next_action`, the menu renders it, and public status/chronicle exports carry it.
- **daily-run-session-extraction** — shipped. `src/game/dailyRunSession.js` now creates and completes Daily Rite run state, including room weave, pacing coach, consequence, and share-card generation.

Validation: `npm test` passed 56/56; `npm run smoke` passed; `npm run build` passed.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
