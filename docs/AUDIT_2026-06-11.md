<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-11 -->
<!-- project: solara · type: game -->

# Project Audit — Solara: Sunfall

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 48.8
- Top 3: daily-rite-stakes-ledger · feedback-action-attribution · backend-readiness-runbook

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 10 | 9 | 14.9 | **daily-rite-stakes-ledger** — Give every Daily Rite route segment a visible stake, risk, and consequence ledger. The current Daily Rite room weave already consumes Director segments, but it mostly explains rooms one beat at a time. A route-level stakes ledger turns the run into a strategic myth contract before the first wave and makes every death, clear, and share feel authored by the sun rather than by a generic dungeon. **Recipe:** Add a deterministic `dailyRiteStakes` module, attach its summary to `createDailyRiteRun`, export it in public chronicle/status, and cover the contract in tests. |
| 2 | 🔥 | user feedback loop | 1h | 9 | 8 | 13.2 | **feedback-action-attribution** — Turn local feedback events into a zero-token action attribution loop. Solara now records public-safe local events, but the digest only counts broad categories. Adding action/source attribution lets the game learn which guidance actually moves a player without collecting private payloads or paying for inference. **Recipe:** Extend `feedbackLedger` events and summaries with sanitized `action_id` and `source`, then add tests proving privacy, caps, and next-action aggregation remain zero-token. |
| 3 | ⚡ | security | 1h | 8 | 7 | 10.2 | **backend-readiness-runbook** — Make the `PG_CONNECTION_SOLARA` gate impossible to misread in public status. The repo has converged on the right project-scoped database gate, but public integration surfaces still compress readiness into prose. A structured runbook block can distinguish missing secret, workflow not run, RPC not hardened, and live-ready states without leaking credentials. **Recipe:** Expand `buildBackendReadiness` with `blocked_by`, `verification_command`, `workflow`, and `scale_posture`, then surface those fields in generated status and tests. |
| 4 | ⚡ | speed / organization / efficiency | 1h | 7 | 6 | 7.7 | **studio-protocol-drift-shims** — Add local compatibility shims for missing Studio protocol scripts observed during `/start`. This session hit missing `skill-profile`, `credential-watch`, `ark`, and brief-staleness scripts before reaching the validated brief. Lightweight no-op shims keep public repos aligned with the Studio command surface while preserving the public/private boundary. **Recipe:** Add safe local scripts that return explicit no-op JSON/status for absent private overlays, credential watch, Ark drain, and brief freshness. |
| 5 | ⚡ | UI / UX / user-experience | 2h | 7 | 5 | 2.8 | **daily-rite-status-component** — Extract the Daily Rite sidebar status into a focused component. `src/App.jsx` remains the biggest drag on iteration, and the Daily Rite panel is now dense with run, vow, challenge, pacing, and share logic. Extracting the display surface creates a smaller future edit target without changing the run loop. **Recipe:** Move the read-only Daily Rite status display into `src/components/DailyRiteStatus.jsx` and keep copy/share handlers in `App.jsx` until a later interaction extraction. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- daily-rite-stakes-ledger
- feedback-action-attribution

## Skipped (Priority < 1.5)

- paid-ai-oracle-summary — Rejected because Solara's standing decision keeps browser and default runtime token cost at zero.
- generic-supabase-repoint — Rejected because the 2026-06-07 decision requires project-scoped `PG_CONNECTION_SOLARA`, not a generic shared database credential.

## Execution Log

- `studio-protocol-drift-shims` — shipped. Added explicit no-op shims for skill-profile, credential-watch, Ark drain, and brief staleness; startup brief render and validation pass.
- `backend-readiness-runbook` — shipped. Backend readiness now exports `blocked_by`, `verification_command`, `workflow`, and `scale_posture`; chronicle tests pin `PG_CONNECTION_SOLARA`.
- `feedback-action-attribution` — shipped. Feedback ledger stores sanitized action/source labels and aggregate attribution with `token_cost: 0`; unit tests cover privacy and chronicle export.
- `daily-rite-stakes-ledger` — shipped. New Daily Rite stakes module is attached to runs and exported through chronicle/status; tests cover `token_cost: 0` and segment coverage.
- `daily-rite-status-component` — shipped. Daily Rite sidebar display moved into `src/components/DailyRiteStatus.jsx`; build and smoke pass with the new component stubbed in the Node harness.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
