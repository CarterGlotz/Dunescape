<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-12 -->
<!-- project: solara · type: game -->

# Project Audit — Solara: Sunfall

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 44.8
- Top 3: daily-rite-outcome-runtime-contract · daily-rite-receipt-ui · daily-rite-decision-window-digest

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed / organization / efficiency | 1h | 9 | 8 | 13.1 | **daily-rite-outcome-runtime-contract** — Extract Daily Rite room outcome application out of App.jsx. S68 made room-clear receipts deterministic, but the runtime still applies HP, Prayer, coin, item, and log effects inline inside the main React loop. A pure application contract keeps App.jsx from becoming the mechanics source of truth and gives tests a stable target for reward safety. **Recipe:** Add `src/game/dailyRiteRoomRuntime.js` that converts a room outcome receipt into bounded player mutations, item grants, sanitized log lines, and a feedback event shape; wire `App.jsx` to use it. |
| 2 | ⚡ | UI / UX / user-experience | 1h | 8 | 7 | 10.2 | **daily-rite-receipt-ui** — Show the latest Daily Rite receipt in the active run panel. Room outcomes currently flash through the combat log, which is easy to miss while the player is fighting. The Daily Rite status panel should carry the latest receipt, reward bundle, and next action so the run explains its consequence without requiring scrollback. **Recipe:** Extend `getDailyRiteStatusContract` and `DailyRiteStatus.jsx` to expose/render `latest_outcome` with coins, recovery, item, and next-action fields while preserving compact layout. |
| 3 | 🔥 | gamification | 1h | 8 | 9 | 11.7 | **daily-rite-decision-window-digest** — Publish deterministic Daily Rite decision windows. The public outcome digest says which segment is richest or safest, but not where a player should actively change behavior. Solara can make the route feel authored by surfacing recovery windows, cache windows, and shrine bargain windows as zero-token decision points for the browser and Studio surfaces. **Recipe:** Add a sanitized decision-window list to the Daily Rite outcome digest and export it through `chronicle.json` / `status.json` with `token_cost: 0`. |
| 4 | ⚡ | security | 30m | 7 | 7 | 9.8 | **daily-rite-outcome-validation** — Prove outcome application stays bounded and public-safe. Reward receipts now affect player resources, so the safety bar should cover more than deterministic generation. Tests should prove the runtime application clamps HP/Prayer, grants only sanitized item ids/counts, emits no unsafe log text, and keeps browser token cost at zero. **Recipe:** Add unit/smoke assertions for the new runtime contract, visible active receipt contract, and public decision-window digest. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- daily-rite-outcome-runtime-contract
- daily-rite-decision-window-digest

## Skipped (Priority < 1.5)

- browser-playwright-daily-rite-receipt-flow — Still valuable, but this repo does not yet carry a browser runner; keep this pass dependency-free.
- generative-room-narration — Rejected by deterministic-first policy and zero browser token cost.

## Execution Log

- `daily-rite-outcome-runtime-contract` — shipped. Added `src/game/dailyRiteRoomRuntime.js` to bound HP/Prayer mutations, sanitize item grants/log lines, emit zero-token reward summaries, and return a public-safe `daily_rite_room_clear` feedback event; `src/App.jsx` now uses it for room clears.
- `daily-rite-receipt-ui` — shipped. `getDailyRiteStatusContract` now exposes `latest_outcome`, and `DailyRiteStatus.jsx` renders the latest clear receipt, reward summary, and next action during active/completed Daily Rite states.
- `daily-rite-decision-window-digest` — shipped. `buildDailyRiteOutcomeDigest` now publishes zero-token `decision_windows`; regenerated `public/chronicle.json` and `public/status.json` include recovery/cache/tempo windows.
- `daily-rite-outcome-validation` — shipped. Added unit and smoke assertions for bounded outcome application, visible active receipt contracts, public decision windows, and zero-token guarantees. Validation passed: 60 unit tests, production build, smoke.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
